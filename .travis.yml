language: python
python: "2.7"
before_install:
  - apt-get update -qq
  - apt-get install -qq libhdf5-7
install:
  - pip install cython  # For PyTables
  - pip install numexpr  # For PyTables
  - pip install -r requirements-travis.txt
script: nosetests tests.core_tests tests.init_country_options_test tests.legislation_tests
